# Overview

This lab implements a COBOL counter-controlled loop using `PERFORM VARYING`.

The source program is structured into three paragraphs:

```text
1000-INICIO
2000-PROCESO
3000-FIN
```

The input message is read once from `SYSIN` and displayed repeatedly by the process paragraph.
