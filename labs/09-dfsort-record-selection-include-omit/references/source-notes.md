# Source notes

This lab is based on the user's hands-on ADCD z/OS evidence and the corresponding DFSORT selection videos from the course sequence.

The implemented examples are adapted to the local lab datasets:

- `IBMUSER.COBOL.FILE80(CLIENTE1)` as the input member.
- `IBMUSER.COBOL.FILE80(CLI600)` as the output for `INCLUDE`.
- `IBMUSER.COBOL.FILE80(CLI399)` as the output for `OMIT`.
- `IBMUSER.ML.JCL(SEL600)` and `IBMUSER.ML.JCL(OMT400)` as the JCL members.

No video files or video-derived screenshots are included in this repository lab.
