# 08 - Índice de evidencias

Evidencias guardadas en:

```text
evidence/screenshots/
evidence/raw/
```

El DOCX subido contiene capturas de `CATCLI`, `CLIENTE1`, `CLIENTE2`, lista de miembros de `IBMUSER.COBOL.FILE80`, `VERCLI1`, `CLIENTE3`, `SORTCLI` y `CLIENTE` final ordenado.
